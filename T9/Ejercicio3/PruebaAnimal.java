package T9.Ejercicio3;

public class PruebaAnimal {
  public static void main(String[] args) {
    // Creamos un objeto de la clase Gato
    Gato miGato = new Gato();

    // Llamamos a algunos métodos de la clase Gato
    miGato.maullar();
    miGato.afilarGarras();
    miGato.ronronear();

    // Creamos un objeto de la clase Perro
    Perro miPerro = new Perro();

    // Llamamos a algunos métodos de la clase Perro
    miPerro.ladrar();
    miPerro.moverCola();
    miPerro.olfatear();

    // Creamos un objeto de la clase Canario
    Canario miCanario = new Canario();

    // Llamamos a algunos métodos de la clase Canario
    miCanario.cantar();
    miCanario.picotear();
    miCanario.revolotear();
  }
}
  
